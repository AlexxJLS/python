peso_limite = float(input("Insira o peso limite:"))
orçamento = float(input("Insira o orçamento:"))

while peso_limite >= 0 and orçamento >= 0:
    peso_produto = float(input("Insira o peso do produto:"))
    preço = float(input("Insira o preço do produto:"))
    
